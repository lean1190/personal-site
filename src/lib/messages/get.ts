'use server';

// import { supabaseServerClient } from '../supabase/server';

export const getMessages = async () => [];
// const { data: messages } = await supabaseServerClient
//     .from('messages')
//     .select()
//     .order('created_at', { ascending: true });

